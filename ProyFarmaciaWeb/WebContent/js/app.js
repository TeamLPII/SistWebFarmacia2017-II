/**
 * 
 */

// Validación Login

$("#btnlogin").click(function() {
	var rpt = $("#respuesta").val();
	console.log(""+rpta);
});

//Función para los parámetros en el modal

selEmpleado=function(nombre, appaterno){
	$('#txteditnombres').val(nombre);
	$('#txteditappaterno').val(appaterno);
}
